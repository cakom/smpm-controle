cat > /home/claude/SMPM-backend-fixed/.env.exemplo << 'EOF'
# Servidor
PORT=4000
NODE_ENV=development

# MongoDB Atlas - Substitua com suas credenciais
MONGODB_URI=mongodb+srv://usuario:senha@cluster.xxxxx.mongodb.net/smpm-controle?retryWrites=true&w=majority

# JWT - Gere uma chave segura
JWT_SECRET=mude_para_uma_chave_secreta_forte_e_unica

# CORS - URLs permitidas (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Frontend URL (para documentação)
VITE_API_URL=http://localhost:4000/api
VITE_APP_NAME=SMPM - Manutenção Preventiva
EOF
echo "✅ .env.exemplo criado"